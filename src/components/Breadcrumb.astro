---
const { items = [] } = Astro.props;
---
<nav class="flex items-center gap-2 text-sm">
  {items.map((item, i) => (
    <>
      <a href={item.href} class="text-indigo-600 hover:underline">{item.label}</a>
      {i < items.length - 1 && <span class="text-gray-400">/</span>}
    </>
  ))}
</nav>
