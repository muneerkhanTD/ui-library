---
const { direction = 'column', gap = 'md', children } = Astro.props;
const gapMap = { sm: 'gap-2', md: 'gap-4', lg: 'gap-8' };
---
<div class={`flex flex-${direction} ${gapMap[gap] || ''}`}>
  {children}
</div>
